# frozen_string_literal: true

Gem::Specification.new do |s|
  s.name    = 'some_gem'
  s.version = '0.0.1'
  s.authors = ['Andrew Jorgensen']
  s.email   = ['andrew@andrewjorgensen.com']
  s.summary = 'Has multiple dependencies from the same repo'
end
